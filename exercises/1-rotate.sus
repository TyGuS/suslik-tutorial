// rotate values of three pointers

void rotate(loc x, loc y, loc z)
{ emp } // TODO: replace emp with a 3-location heap
{ emp } // TODO: replace emp with a 3-location heap, with values rotated
{
  ??
}